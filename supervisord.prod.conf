# ============================================================
# 生产环境 supervisord 配置（数据库已拆分，只跑 .NET + Nginx）
# ============================================================

[supervisord]
nodaemon=true
user=root
logfile=/var/log/supervisor/supervisord.log
pidfile=/var/run/supervisord.pid

[program:dotnetapp]
command=/usr/bin/dotnet /app/publish/CarAssemblyErp.dll
user=root
directory=/app/publish
autostart=true
autorestart=true
priority=10
startsecs=5
startretries=10
environment=ASPNETCORE_URLS="http://0.0.0.0:5000"
stdout_logfile=/var/log/supervisor/dotnetapp.log
stderr_logfile=/var/log/supervisor/dotnetapp.err

[program:nginx]
command=/usr/sbin/nginx -g 'daemon off;'
user=root
autostart=true
autorestart=true
priority=20
startsecs=1
stdout_logfile=/var/log/supervisor/nginx.log
stderr_logfile=/var/log/supervisor/nginx.err
